
export default class RecommendedDosageForWeek {

    static ofDao(infoDto) {
        let info = {};
        return info;
    }

    static toDao(info) {
        let dao = {};
        return dao;
    }
}